---
pageable: true
per_page: 10
---
- page_articles.each_with_index do |article, i| 
  %article
    %h1
      = link_to article.title, article
      %span= I18n.localize(article.date, format: :short)
    %p= article.summary

- if paginate 
  .pagination
    %p
      Página 
      = page_number
      de
      = num_pages 

    - if prev_page
      %p= link_to 'Página anterior', prev_page

    - if next_page
      %p= link_to 'Siguiente página', next_page
